apiVersion: v1
kind: Service
metadata:
  name: endpoints
spec:
  type: LoadBalancer
  selector:
    app: kubernetes-series
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
